{
  "schema_version": 2,
  "generated_at": "2026-07-30T16:31:04Z",
  "protocol_epoch": "security_v2_cutover",
  "release_identity": {
    "chain_id": 1,
    "cutover_block": 0,
    "notes": "The chain id and the cutover block are held against what the client compiles in, so this document can never name a cutover height no node observes. The zero cutover block is the compiled placeholder a reviewed release commit must replace with the block published in the operator notice; `keep-client release-manifest validate` accepts this document as internally valid, and `validate --release-ready` refuses it until that block is set. What the release was built into is deliberately absent here rather than unrecorded: the commit finally built and the immutable image digests are outputs of a build over these very bytes, so recording them would require this file to contain a hash of the tree containing it — writing the value changes the commit the value names. They belong to the detached release provenance, generated after the build, never committed to the tree it describes, and bound back to this document by its hash; `release-manifest verify-provenance` checks the pair. No release-acceptance decision may be taken against a manifest that does not pass `--release-ready`, nor without provenance verifying against it."
  },
  "termination_grace": {
    "tbtc_completion_blocks": 1200,
    "beacon_completion_blocks": 136,
    "beacon_inputs": {
      "group_size": 64,
      "result_publication_block_step": 1,
      "relay_entry_timeout_blocks": 64
    },
    "maximum_legacy_completion_blocks": 1200,
    "reviewed_margin_blocks": 100,
    "upper_block_interval_seconds": 15,
    "rpc_processing_allowance_seconds": 300,
    "in_process_backstop_seconds": 19800,
    "forced_cancellation_allowance_seconds": 300,
    "process_exit_headroom_seconds": 60,
    "termination_grace_period_seconds": 20160,
    "notes": "Generated by `keep-client release-manifest derive`. The grace is a ceiling, not a wait: a node whose drain completes exits immediately, and only a node still finishing already-started protocol work uses it. The forced-cancellation allowance covers the audited forced-cancellation path that runs after the in-process backstop fires — canceling outlived permits, persisting their audit records, closing the gate — before the service manager may escalate to SIGKILL; 300 s mirrors the RPC/processing margin inside the backstop. The process-exit headroom covers the shutdown work outside both in-process timers — controller scheduling and the quiesce call before the backstop arms, the gate close between the timers, and the logging, run-context teardown, and process exit after them — because the service manager counts the grace from signal delivery while the timers start later; without it a cleanup consuming its full allowance could still be SIGKILLed mid-teardown. Every number here is compiled into the client — the allowance itself is the compiled constant the runtime's forced-cancellation cleanup wait consumes, and validation rejects a manifest recording any other allowance, even one whose grace was recomputed coherently around it. Any change to a compiled bound, the allowance included, requires regenerating this manifest with `derive` and re-reviewing it; `go test ./cmd/ -run TestReleaseManifest` and `keep-client release-manifest validate` both reject a stale copy."
  }
}

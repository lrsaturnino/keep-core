---
apiVersion: v1
kind: Service
metadata:
  name: alertmanager
spec:
  type: ClusterIP
  selector:
    app: alertmanager
    type: monitoring
  ports:
    - name: alertmanager
      port: 9093
      targetPort: alertmanager
      protocol: TCP

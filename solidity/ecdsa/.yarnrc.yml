nodeLinker: node-modules

# eth-sig-util@1.4.2 still declares ethereumjs-abi as a git URL on npm; we pin the
# registry tarball so PR hardened mode and `yarn install --immutable` agree.
packageExtensions:
  "eth-sig-util@1.4.2":
    dependencies:
      ethereumjs-abi: "npm:0.6.8"
